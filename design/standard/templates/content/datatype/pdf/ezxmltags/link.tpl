{* DO NOT EDIT THIS FILE! Use an override template instead. *}{* ezurl without (no) returns the address already wrapped in quotes, ready for an href attribute; this goes into the pdf link directive instead, so the quotes ended up inside the address and every link pointed at a url that began and ended with a quotation mark. Everything here is on one line, with no newline at the end, on purpose: a parent tag such as emphasize washes the rendered output of its children, so any newline in this file becomes a line break in the finished page. *}{pdf(link, hash(url, $href|ezurl(no), text, $content|wash(pdf)))}