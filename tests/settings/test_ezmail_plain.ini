# Mail test settings for SMTP/IMAP-backed integration tests.
# Fill these in with a real test mailbox on a provider you control.
# Credentials stay out of git; the committed defaults intentionally remain blank.
[MailSettings]
Transport=SMTP
TransportServer=
TransportConnectionType=
TransportPort=25
TransportUser=
TransportPassword=